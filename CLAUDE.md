# CLAUDE.md — правила работы в репозитории aivox-core

## Git workflow

После каждого `git commit` в этом репозитории автоматически выполняется `git push origin main`,
если пользователь явно не попросил не пушить. Результат push включается в отчёт о задаче.
